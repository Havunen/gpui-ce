[package]
name = "gpui_ce_windows_bindings"
version = "0.1.0"
edition.workspace = true
publish = false

[dependencies]
# Keep the generator compatible with the windows-core 0.62 runtime.
windows-bindgen = "=0.100.0"
